---
import '../styles/global.css';
import { BASE_URL } from '../consts';
interface Props {
  title: string;
  active: 'accueil' | 'marche' | 'modules' | 'abonnement';
}
const { title, active } = Astro.props;
const tabs = [
  { id: 'accueil',    label: 'Accueil',     href: `${BASE_URL}/`,                  icon: 'M3 11.5 12 4l9 7.5M5 10v9h14v-9' },
  { id: 'marche',     label: 'Marché',      href: `${BASE_URL}/marche/`,           icon: 'M3 17l5-5 4 4 8-9M3 21h18' },
  { id: 'modules',    label: 'Modules',     href: `${BASE_URL}/modules/`,          icon: 'M4 5h16v14H4zM4 9h16M9 5v14' },
  { id: 'abonnement', label: 'Abonnement',  href: `${BASE_URL}/abonnement/`,       icon: 'M12 3v18M7 7h7a3 3 0 0 1 0 6H7m0 0h8a3 3 0 0 1 0 6H7' },
];
---
<!doctype html>
<html lang="fr">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover" />
  <meta name="theme-color" content="#0C0B09" />
  <meta name="apple-mobile-web-app-capable" content="yes" />
  <meta name="apple-mobile-web-app-status-bar-style" content="black-translucent" />
  <meta name="apple-mobile-web-app-title" content="FormationVVS" />
  <title>{title} — FormationVVS</title>
  <link rel="preconnect" href="https://api.fontshare.com" crossorigin />
  <link rel="preconnect" href="https://cdn.fontshare.com" crossorigin />
  <link rel="stylesheet" href="https://api.fontshare.com/v2/css?f[]=clash-display@400,500,600,700&f[]=satoshi@400,500,700&display=swap" />
  <link rel="manifest" href={`${BASE_URL}/manifest.webmanifest`} />
  <link rel="apple-touch-icon" href={`${BASE_URL}/icon-192.png`} />
  <link rel="icon" type="image/svg+xml" href={`${BASE_URL}/favicon.svg`} />
  <script is:inline>
    // Destruction totale de tout service worker résiduel (évitait les 404 sur navigation).
    if ('serviceWorker' in navigator) {
      navigator.serviceWorker.getRegistrations().then((regs) => {
        regs.forEach((r) => r.unregister());
      }).catch(() => {});
    }
    if ('caches' in window) {
      caches.keys().then((keys) => keys.forEach((k) => caches.delete(k))).catch(() => {});
    }
  </script>
  <script>
    import '../scripts/motion.js';
  </script>
</head>
<body>
  <!-- Header app -->
  <header class="app-header">
    <div class="app-header__inner">
      <a href={`${BASE_URL}/`} class="brand">
        <span class="brand__mark">FV</span>
        <span class="brand__name">FORMATION<span class="brand__vvs">VVS</span></span>
      </a>
      <a href={`${BASE_URL}/abonnement/`} class="app-header__cta">Rejoindre</a>
    </div>
  </header>

  <main class="app-main">
    <slot />
  </main>

  <!-- Bottom tab bar -->
  <nav class="tabbar" aria-label="Navigation principale">
    {tabs.map((t) => (
      <a
        href={t.href}
        class={`tabbar__item ${active === t.id ? 'is-active' : ''}`}
        aria-current={active === t.id ? 'page' : undefined}
      >
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
          <path d={t.icon} />
        </svg>
        <span>{t.label}</span>
      </a>
    ))}
  </nav>

  <!-- Footer global -->
  <footer class="app-footer">
    <div class="app-footer__inner">
      <div class="app-footer__col">
        <a href={`${BASE_URL}/`} class="brand brand--footer">
          <span class="brand__mark">FV</span>
          <span class="brand__name">FORMATION<span class="brand__vvs">VVS</span></span>
        </a>
        <p class="app-footer__tag">Former brutal, pas bruit. Méthode, risque, setups en live.</p>
      </div>
      <div class="app-footer__col">
        <h4>Naviguer</h4>
        <a href={`${BASE_URL}/marche/`}>Marché</a>
        <a href={`${BASE_URL}/modules/`}>Modules</a>
        <a href={`${BASE_URL}/abonnement/`}>Abonnement</a>
      </div>
      <div class="app-footer__col app-footer__contact">
        <h4>Contact</h4>
        <a href="mailto:contact@formationvvs.fr">contact@formationvvs.fr</a>
        <a href="https://discord.gg/formationvvs">Discord</a>
        <a href="https://t.me/formationvvs">Telegram</a>
      </div>
    </div>
    <div class="app-footer__base">
      <span>© {new Date().getFullYear()} FormationVVS</span>
      <span>Tous droits réservés</span>
    </div>
  </footer>

  <style>
    .app-header {
      position: sticky; top: 0; z-index: 50;
      background: var(--charbon); /* opaque par défaut — jamais de flash/blanc sur Safari */
      border-bottom: var(--border-w) solid var(--ligne);
      padding-top: var(--safe-t);
    }
    @supports ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      .app-header { background: color-mix(in srgb, var(--charbon) 88%, transparent); -webkit-backdrop-filter: blur(12px); backdrop-filter: blur(12px); }
    }
    .app-header__inner {
      display: flex; align-items: center; justify-content: space-between;
      max-width: 1080px; margin: 0 auto; padding: 12px 16px;
    }
    .brand { display: flex; align-items: center; gap: 10px; }
    .brand__mark {
      font-family: var(--font-display); font-weight: 700;
      background: var(--orange); color: var(--charbon);
      width: 36px; height: 36px; display: grid; place-items: center;
      border-radius: 7px; font-size: 0.95rem; letter-spacing: -0.05em;
    }
    .brand__name { font-family: var(--font-display); font-weight: 700; font-size: 0.92rem; letter-spacing: -0.02em; text-transform: uppercase; }
    .brand__vvs { color: var(--orange); }
    .app-header__cta {
      font-family: var(--font-display); font-weight: 600; text-transform: uppercase;
      font-size: 0.8rem; padding: 8px 14px; border: var(--border-w) solid var(--orange);
      border-radius: 7px; color: var(--orange); transition: all .12s ease;
    }
    .app-header__cta:hover { background: var(--orange); color: var(--charbon); }

    .app-main { min-height: 70vh; }

    .tabbar {
      position: fixed; left: 0; right: 0; bottom: 0; z-index: 60;
      display: flex; justify-content: space-around; align-items: stretch;
      background: var(--charbon-2); /* opaque par défaut — jamais de flash sur Safari */
      border-top: var(--border-w) solid var(--ligne);
      padding-bottom: var(--safe-b);
    }
    @supports ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
      .tabbar { background: color-mix(in srgb, var(--charbon-2) 95%, transparent); -webkit-backdrop-filter: blur(14px); backdrop-filter: blur(14px); }
    }
    .tabbar__item {
      flex: 1; display: flex; flex-direction: column; align-items: center; gap: 3px;
      padding: 9px 0 7px; color: var(--texte-2);
      font-family: var(--font-body); font-weight: 700; font-size: 0.62rem;
      text-transform: uppercase; letter-spacing: 0.04em;
      transition: color .12s ease, transform .12s ease;
      position: relative;
    }
    .tabbar__item svg { width: 23px; height: 23px; }
    .tabbar__item.is-active { color: var(--orange); }
    .tabbar__item.is-active::before {
      content: ''; position: absolute; top: 0; left: 50%; transform: translateX(-50%);
      width: 26px; height: 3px; background: var(--orange); border-radius: 0 0 3px 3px;
    }
    .tabbar__item:active { transform: scale(0.94); }

    .app-footer {
      border-top: var(--border-w) solid var(--ligne);
      background: var(--charbon);
      padding: 40px 20px calc(76px + var(--safe-b)); /* laisse place à la tabbar fixe */
      margin-top: 40px;
    }
    .app-footer__inner {
      max-width: 1080px; margin: 0 auto;
      display: grid; grid-template-columns: 1.6fr 1fr 1fr; gap: 28px;
    }
    .brand--footer { margin-bottom: 14px; }
    .app-footer__tag { color: var(--texte-2); font-size: 0.9rem; max-width: 34ch; line-height: 1.5; }
    .app-footer__col h4 {
      font-family: var(--font-body); font-weight: 700; font-size: 0.7rem;
      text-transform: uppercase; letter-spacing: 0.08em; color: var(--texte-2);
      margin: 0 0 12px;
    }
    .app-footer__col a {
      display: block; color: var(--texte); font-size: 0.9rem; font-weight: 500;
      padding: 5px 0; transition: color .12s ease;
    }
    .app-footer__col a:hover { color: var(--orange); }
    .app-footer__base {
      max-width: 1080px; margin: 32px auto 0; padding-top: 20px;
      border-top: 1px solid var(--ligne);
      display: flex; justify-content: space-between; gap: 12px;
      font-size: 0.78rem; color: var(--texte-2);
    }
    @media (max-width: 760px) {
      .app-footer__inner { grid-template-columns: 1fr 1fr; gap: 24px 16px; }
      .app-footer__col:first-child { grid-column: 1 / -1; }
      .app-footer__base { flex-direction: column; gap: 4px; text-align: left; }
    }
  </style>
</body>
</html>
