---
import BaseLayout from '../layouts/BaseLayout.astro';
import MarketBoard from '../components/MarketBoard.astro';
import ModuleCard from '../components/ModuleCard.astro';
import { modules } from '../data/content';
import { getLang, t, localizedPath } from '../i18n/ui';
const lang = getLang(Astro.url);
---
<BaseLayout title={t(lang, 'home.heroTitle').split('\n')[0]} active="accueil">
  <!-- HERO -->
  <section class="hero wrap">
    <div class="hero__copy" data-reveal>
      <span class="eyebrow">{t(lang, 'home.eyebrow')}</span>
      <h1 set:html={t(lang, 'home.heroTitle').replace(/\n/g, '<br/>')} />
      <p class="hero__lead">{t(lang, 'home.heroLead')}</p>
      <div class="hero__cta">
        <a href={localizedPath('/modules/', lang)} class="btn">{t(lang, 'home.ctaModules')}</a>
        <a href={localizedPath('/abonnement/', lang)} class="btn btn--ghost">{t(lang, 'home.ctaAbo')}</a>
      </div>
    </div>
  </section>

  <!-- MÉTHODE -->
  <section class="section wrap">
    <div class="sec-head" data-reveal>
      <span class="eyebrow">{t(lang, 'home.methodeEyebrow')}</span>
      <h2>{t(lang, 'home.methodeTitle')}</h2>
      <p>{t(lang, 'home.methodeLead')}</p>
    </div>
    <ol class="method">
      <li data-reveal><span class="method__n">01</span><div><h3>{t(lang, 'home.m1t')}</h3><p>{t(lang, 'home.m1d')}</p></div></li>
      <li data-reveal data-reveal-delay="80"><span class="method__n">02</span><div><h3>{t(lang, 'home.m2t')}</h3><p>{t(lang, 'home.m2d')}</p></div></li>
      <li data-reveal data-reveal-delay="160"><span class="method__n">03</span><div><h3>{t(lang, 'home.m3t')}</h3><p>{t(lang, 'home.m3d')}</p></div></li>
      <li data-reveal data-reveal-delay="240"><span class="method__n">04</span><div><h3>{t(lang, 'home.m4t')}</h3><p>{t(lang, 'home.m4d')}</p></div></li>
    </ol>
  </section>

  <!-- MARCHÉ -->
  <section class="section wrap">
    <div class="sec-head" data-reveal>
      <span class="eyebrow">{t(lang, 'home.marcheEyebrow')}</span>
      <h2>{t(lang, 'home.marcheTitle')}</h2>
      <p>{t(lang, 'home.marcheLead')}</p>
    </div>
    <div class="market-block" data-reveal>
      <div class="board-head">
        <span class="board-head__t">{t(lang, 'marche.boardTitle')}</span>
        <span class="board-head__dot"></span>
      </div>
      <MarketBoard />
      <a href={localizedPath('/marche/', lang)} class="market-link">{t(lang, 'home.marcheLink')}</a>
    </div>
  </section>

  <!-- MODULES -->
  <section class="section wrap">
    <div class="sec-head" data-reveal>
      <h2>{t(lang, 'home.cursusTitle')}</h2>
      <p>{t(lang, 'home.cursusLead')}</p>
    </div>
    <div class="mods">
    {modules.map((m) => <ModuleCard {...m} href={localizedPath(`/modules/${m.slug}/`, lang)} />)}
    </div>
  </section>

  <!-- CTA -->
  <section class="section wrap">
    <div class="panel-orange cta-panel" data-reveal>
      <h2>{t(lang, 'home.ctaPanelTitle')}</h2>
      <p>{t(lang, 'home.ctaPanelLead')}</p>
      <a href={localizedPath('/abonnement/', lang)} class="btn">{t(lang, 'home.ctaPanelBtn')}</a>
    </div>
  </section>
</BaseLayout>

<style>
  .hero {
    display: flex; flex-direction: column; gap: 24px;
    padding: 64px 20px 48px; max-width: 760px; margin: 0 auto;
  }
  .hl { color: var(--orange); }
  .hero__lead { color: var(--texte-2); font-size: 1.1rem; margin: 22px 0 30px; max-width: 52ch; line-height: 1.6; }
  .hero__cta { display: flex; flex-wrap: wrap; gap: 12px; }

  .board-head { display: flex; align-items: center; justify-content: space-between; margin-bottom: 14px; }
  .board-head__t { font-family: var(--font-display); font-weight: 700; font-size: 0.78rem; letter-spacing: 0.06em; text-transform: uppercase; }
  .board-head__dot { width: 9px; height: 9px; border-radius: 50%; background: var(--emeraude); box-shadow: 0 0 0 4px color-mix(in srgb, var(--emeraude) 25%, transparent); animation: pulse 2s infinite; }
  @keyframes pulse { 50% { opacity: 0.4; } }

  .market-block {
    background: var(--charbon-2); border: var(--border-w) solid var(--ligne);
    border-radius: var(--radius); padding: 20px; box-shadow: var(--ombre-dure);
  }
  .market-link { display: inline-block; margin-top: 16px; color: var(--orange); font-weight: 700; font-size: 0.85rem; }

  .method { list-style: none; display: grid; grid-template-columns: repeat(2, 1fr); gap: 12px; }
  .method li {
    display: flex; gap: 14px; align-items: flex-start;
    background: var(--charbon-2); border: var(--border-w) solid var(--ligne);
    border-radius: var(--radius); padding: 16px 18px;
  }
  .method__n { font-family: var(--font-display); font-weight: 700; font-size: 1.2rem; color: var(--orange); letter-spacing: -0.04em; flex: 0 0 auto; }
  .method h3 { font-size: 1.05rem; margin: 0 0 4px; }
  .method p { font-size: 0.85rem; color: var(--texte-2); margin: 0; }

  .sec-head { margin-bottom: 32px; }
  .sec-head p { color: var(--texte-2); margin-top: 10px; max-width: 52ch; }
  .mods { display: grid; grid-template-columns: repeat(2, 1fr); gap: 16px; }

  .cta-panel { padding: 40px; text-align: center; }
  .cta-panel p { max-width: 44ch; margin: 14px auto 26px; color: #2A1500; font-weight: 500; }

  @media (max-width: 1024px) {
    .hero { padding-top: 48px; }
    .board { grid-template-columns: 1fr !important; }
    :global(.board .tile) { flex-direction: row !important; align-items: center; gap: 12px; }
  }
  @media (max-width: 760px) {
    .hero { padding: 40px 16px 32px; }
    .hero__lead { font-size: 1.05rem; }
    .hero__cta { flex-direction: column; align-items: stretch; }
    .hero__cta .btn { width: 100%; justify-content: center; }
    .method { grid-template-columns: 1fr; }
    .mods { grid-template-columns: 1fr; }
    .cta-panel { padding: 30px 22px; }
    .market-block { padding: 16px; }
  }
  @media (max-width: 640px) {
    .hero h1 { font-size: clamp(2.3rem, 11vw, 3.4rem); }
    .sec-head { margin-bottom: 24px; }
  }
</style>
